# Contributing

This plugin is part of [dsh-wsl-kit](https://github.com/173787247/dsh-wsl-kit).

1. Keep changes small and covered by `npm test`.
2. Prefer pure helpers in `lib/budget.js` over growing `index.js`.
3. Do not log secrets; block messages are user-visible in Trajectory.